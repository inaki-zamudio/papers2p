# Resumen: «A Simple Technique for Handling Multiple Polymorphism» – Daniel H. H. Ingalls

## 📌 Introducción
El artículo aborda el problema del **polimorfismo múltiple** en programación orientada a objetos, donde más de un término en una expresión puede ser de tipo variable. Esto rompe la modularidad y lleva a código difícil de mantener y extender.

---

## 🔁 Polimorfismo y Mensajes
- El paradigma de mensajes resuelve el polimorfismo simple: el receptor de un mensaje puede variar en tipo, y el envío del mensaje selecciona automáticamente el método adecuado.
- Esto evita la explosión combinatoria de comprobar tipos manualmente y mantiene la modularidad.

---

## ❓ El Problema: Polimorfismo Múltiple
Cuando **más de un parámetro** es polimórfico, el envío de mensajes convencional falla.  
**Ejemplo**:  
- **Objetos gráficos**: Rectángulo, Óvalo, Bitmap, Texto…  
- **Puertos de visualización**: Pantalla, Impresora, Remoto…

Si usamos comprobación de tipos en el método `displayOn:`, el código se vuelve frágil y no modular:

```smalltalk
<Rectangle> displayOn: aPort
    aPort isMemberOf: DisplayPort ifTrue: [ ... ]
    aPort isMemberOf: PrinterPort ifTrue: [ ... ]
    ...
```

Esto dificulta añadir nuevos tipos de puertos o gráficos.

---

## 💡 La Solución: Doble Despacho (Double Dispatch)
La clave es **enviar un segundo mensaje** que despache según el tipo del argumento.

### Paso 1: El objeto gráfico envía un mensaje específico al puerto
```smalltalk
<Rectangle> displayOn: aPort
    aPort displayRectangle: self

<Oval> displayOn: aPort
    aPort displayOval: self
```

### Paso 2: Cada puerto implementa métodos para cada tipo de gráfico
```smalltalk
<DisplayPort> displayRectangle: aRect
    "Dibujar rectángulo en pantalla"

<PrinterPort> displayRectangle: aRect
    "Imprimir rectángulo en impresora"
```

---

## ✅ Ventajas
- **Modularidad**: Añadir un nuevo gráfico solo requiere:
  - Un método `displayOn:` en la nueva clase gráfica.
  - Implementar `displayNuevoGráfico:` en cada puerto.
- Añadir un nuevo puerto es aún más simple: implementar todos los métodos `displayX:`.
- La técnica es generalizable a **más de dos niveles** de polimorfismo.

---

## 🧪 Experiencias de Uso
- Manejadores de eventos con distintos tipos de eventos.
- Unificación en programación lógica.
- Lógica de coerción aritmética en Smalltalk-80.

---

## 🧠 Conclusión
El **doble despacho** es una técnica simple y poderosa para manejar polimorfismo múltiple, manteniendo la modularidad y extensibilidad del código orientado a objetos, sin necesidad de extensiones al lenguaje.

---
*Referencia: OOPSLA '86 Proceedings, Septiembre 1986.*